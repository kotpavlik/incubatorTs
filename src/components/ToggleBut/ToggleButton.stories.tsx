import React from 'react';
import ToggleButton from './ToggleButton';
import {action} from '@storybook/addon-actions';

export default {
    title: 'ToggleButton',
    component: ToggleButton,
};

export const onButton = () => <ToggleButton />

